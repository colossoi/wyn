Remaining work to port wyn-core SSA to wyn-ssa:

1. ControlHeader — SPIR-V needs structured control flow annotations
   (loop/selection merge blocks). Blocks need an optional ControlHeader field,
   or this gets layered on top in wyn-core.

2. Source location — Span and NodeId on instructions. Could be generic or
   stored in a side-table.

3. Block param names — The existing IR stores optional debug names on block
   params. Could be added to ValueInfo or kept external.
