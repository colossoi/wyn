//! All EntryBuilder reduce-port tests retired with the deletion of
//! `phase1_transform_reduce`, `synthesize_phase2_reduce`, and
//! `emit_pending_reduce` in the Screma consolidation. Kept as a stub
//! so the `#[cfg(test)] #[path] mod builder_tests` registration in
//! `builder.rs` still compiles.
