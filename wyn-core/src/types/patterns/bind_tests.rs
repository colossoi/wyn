//! Tests for `bind_pattern` and `fresh_type_for_pattern`. The
//! `bind_pattern` logic was already exercised end-to-end via
//! `types::checker_tests` (let-binding and lambda-param coverage); we
//! keep the integration coverage there and use this file for any
//! patterns/-local unit tests that emerge.

// Placeholder: end-to-end coverage lives in `types::checker_tests`.
