{
  "name": "wyn-egir-viz",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:wasm": "wasm-pack build ../wyn-egir-viz-wasm --target web --out-dir ../wyn-egir-viz/src/wasm-pkg --release --offline",
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:all": "npm run build:wasm && npm run build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vite": "8.0.13"
  }
}
