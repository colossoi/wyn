-- Test compute shader: outputs global invocation ID to debug buffer
#[compute(64, 1, 1)] def compute_main(
  #[builtin(global_invocation_id)] gid: vec3u32
) -> () =
  let _ = debug_u32(gid.x) in
  ()
