-- Cheap value choices become select; division remains guarded at zero.
entry select_lowering(xs: []i32) []i32 =
  map(|x: i32|
    let chosen = if x < 0 then -x else x + 10
    let guarded = if x != 0 then 120 / x else 7
    in chosen + guarded,
    xs)
