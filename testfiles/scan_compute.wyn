-- Scan (prefix sum) compute shader test
-- Computes running sum of an array: [1,2,3,4] → [1,3,6,10]

#[compute]
entry prefix_sum(
    #[size_hint(1024)] data: []f32
) []f32 =
    scan(|a: f32, b: f32| a + b, 0.0, data)
