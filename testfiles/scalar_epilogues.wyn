entry attached(xs: []i32, seed: [1]i32, n: i32) ([]i32, [3]i32, [2]i32) =
  let a = loop acc = seed[0] for i < n do acc + i
  let ys = map(|x: i32| x + a, xs)
  let zs = map(|i: i32| i * a, iota(3))
  let b = loop acc = a for i < n do acc + i * 2 in
  (ys, zs, [a, b])
