-- Reduce compute shader test
-- Sums an array of f32 values using parallel reduction

#[compute]
entry sum_array(
    #[size_hint(1024)] data: []f32
) f32 =
    reduce(|a: f32, b: f32| a + b, 0.0, data)
