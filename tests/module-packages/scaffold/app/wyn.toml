manifest-version = 1

[package]
name = "test/app"
version = "v1.0.0"
wyn = "v0.1.0"
library = "src/main.wyn"

[dependencies]
example = { package = "test/example", version = "v1.0.0", path = "../deps/example" }
